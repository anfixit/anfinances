# Screenshots

Place PNG screenshots here; they are referenced from the main README:

- `dashboard.png` — the overview screen (net worth, cashflow, by-category)
- `budget.png` — the YNAB-style budget with category groups
- `transactions.png` — the transactions list with the add sheet

Recommended: light theme, ~1400px wide, real-looking but non-sensitive data.
