"""Security: password hashing и JWT.

Заглушка. Реализация — в шаге 3 (auth).
Тут будут функции:
- hash_password / verify_password (passlib + bcrypt)
- create_access_token / create_refresh_token (python-jose)
- decode_token

См. ARCHITECTURE.md §5 "Auth-стратегия".
"""
